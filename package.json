{
  "name": "hmetaiji-map",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build && vite build --mode page",
    "build:lib": "vite build",
    "build:page": "vite build --mode page",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.8.0",
    "vite": "^6.0.0"
  },
  "dependencies": {
    "mapbox-gl": "^3.12.0"
  }
}
